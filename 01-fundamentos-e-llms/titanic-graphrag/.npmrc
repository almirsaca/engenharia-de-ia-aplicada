# @langchain/community declara dezenas de peer dependencies opcionais para
# integrações que este laboratório não usa (typeorm, better-sqlite3, ...), e
# duas delas entram em conflito entre si. Nenhuma é necessária aqui: usamos
# apenas Neo4j, OpenRouter e Transformers.js.
legacy-peer-deps=true
